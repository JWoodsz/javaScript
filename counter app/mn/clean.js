// array
const numbers =[43,45,56,67]
const number2 = new array (22,34,56,78)

const fruit = ['apple', 'banana', 'oramge']
const mix = [22, 'hello', true, null, undefined]

let value

val = numbers.length
val = array.isArray(numbers)

val = numbers[3]
val = numbers[0]
numbers[2] = 100
val = numbers[2]
val = numbers.indexOf(36)

numbers.push(250)

numbers.unshift(12)

numbers.pop()

numbers.shift()

numbers.splice

number2.reverse()

val = numbers.concat(numbers2)

val = fruit.sort()
val = numbers.sort()

val = numbers.sort(function(x,y){return x - y})

val = numbers.sort(function(y,x){return y-x})

function under50(num){
    return num < 50
}

val = numbers.find(under50)
val = numbers.find(over50)

